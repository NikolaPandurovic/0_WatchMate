export type RootStackParamList = {
  Collection: undefined;

  // Add = ohne Parameter
  // Edit = mit watch
  AddWatchCollection: {
    watch?: any;
  };

  WatchDetailScreen: {
    watch: any;
  };
};
